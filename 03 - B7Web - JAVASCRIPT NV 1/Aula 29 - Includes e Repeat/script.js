let lista = ['Ovo', 'Café', 'Arroz', 'Feijão', 'Macarrão'];


//Função utilizada para procurar itens retornando true ou false.
//Obs: Essa função também pode ser utilizada em strings.
console.log( lista.includes('Arroz') );

//Exemplo de utilização da função ".includes()" em string.
let nome = 'Edson';

console.log(nome.includes('E'));

//Função repeat em strings.
//Como parâmetro setamos a quantidade de vezes que queremos a repetição.
console.log('x'.repeat(186));